# ============================================================
# Maestro client — environment variables
# ============================================================
#
# Maestro runs against Stellar testnet with the network + contract IDs
# hardcoded in src/config/stellar.ts, so no environment variables are
# required to run the client. This file is kept as a placeholder for any
# future deploy-time configuration.
